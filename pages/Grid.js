import React from 'react'
import styles from './Buttons.module.sass'

const Grid = () => {
  return (
    <>
      <section className={styles.buttons}>
        <h1 className={styles.buttons__title}>Grid</h1>
      </section>
    </>
  )
}

export default Grid
